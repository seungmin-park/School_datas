desc emp;
select deptno, dname from dept;
select dname, loc from dept;
select sal, comm from emp;
select distinct hiredate from emp;
select distinct mgr from emp;
select distinct hiredate from emp;
select distinct deptno from emp;
select sal, sal * 6 from emp;
select comm, comm * 6 from emp;
select ename as NAME, sal as Salary from emp;
select empno as 사원번호, ename as 이름, deptno as 부서번호, hiredate as 입사일자 from emp; 
select deptno as 부서번호, dname as 부서명, loc as 지역명 from dept;
select deptno || ename from emp;
select to_char(hiredate,'yy/MM/dd') || '에 입사한' || ename || '입니다' from emp;
select ename, deptno from emp where deptno = 10;
select empno, ename, sal from emp where sal >= 2000;
select empno, ename, job from emp where job = 'CLERK';
select ename, hiredate from emp where hiredate = '1980-12-17';
select dname, loc,deptno from dept where deptno <> 30;
select hisal, losal from salgrade where grade = 5;
select * from emp where ename = 'WARD';
select * from emp where deptno = 10 and job = 'MANAGER';
select empno, ename, sal, deptno from emp where sal >=2000 and deptno = 30;
select empno, ename job, hiredate from emp where job = 'CLERK' and hiredate > '1981-12-31';
select ename, job, deptno from emp where deptno <> 20 and job = 'MANAGER';
select dname, loc from dept where loc <> 'BOSTON';
select ename, job, sal from emp where job = 'ANALYST' and sal <= 2000;
select empno, ename, sal from emp where sal between 1000 and 2500;
select empno, ename, deptno from emp where empno like '75%';
select ename, sal, deptno from emp where deptno between 10 and 30;
select ename, mgr from emp where mgr like '76%';
select ename, sal, comm, empno from emp where empno like '79%';
select empno, ename, deptno, hiredate from emp where hiredate between '1981-02-01' and '1981-02-28';
select empno, ename from emp where ename like '%A%';
select empno, ename, mgr from emp where mgr is null;
select empno, ename, mgr from emp where mgr is not null;
select empno, ename from emp where empno in(7902,7781);
select empno, ename from emp where mgr in(7698,7839);
select empno, ename, job from emp where job in('MANAGER','SALESMAN');
select empno, ename from emp order by empno;
select empno, ename, deptno from emp order by deptno, ename;
select * from emp order by deptno, sal desc;
select * from emp order by job, sal;
select * from emp order by deptno, job, sal;
select LOWER(ename) from emp;
select lower(ename), lower(job) from emp;
select upper(ename) from emp;
select upper(ename), upper(job) from emp;
select initcap(ename) from emp;
select initcap(ename), initcap(job) from emp;
select concat(ename,job) from emp;
select ename, substr(ename,1,2) from emp;
select ename, job, substr(job,2,3) from emp;
select ename, length(ename) from emp;
select job, length(job) from emp;
select ename, instr(ename,'A',1,1) from emp;
select job, instr(job,'A',1,1) from emp;
select rpad(ename,15,'&') from emp;
select lpad(job,20,'%') from emp;
select empno, ename, round(sal,-2) from emp;
select empno, ename, sal, trunc(sal, -2) from emp;
select empno, sal, mod(sal,100) from emp;
select ename, hiredate, hiredate+100 from emp;
select ename, hiredate, trunc(months_between(sysdate,hiredate),0) from emp;
select ename, hiredate, add_months(hiredate,3) from emp;
select ename, hiredate, next_day(hiredate,6) from emp;
select ename, hiredate, last_day(hiredate) from emp;
select ename, hiredate, round(hiredate, 'year') from emp;
select ename, hiredate, trunc(hiredate, 'year') from emp;
select ename, nvl(to_char(mgr),'상급자없음') from emp;
select empno, ename, sal+nvl(comm,0) from emp;
select empno, ename, (sal+nvl(comm,0)) * 12 from emp;
select empno, ename, nvl(to_char(mgr),'CEO') from emp;

select empno, ename, sal, case job when 'SALESMAN' then sal *1.1 when 'MANAGER' then sal * 1.2 when 'CLERK'  then sal *1.15 end as 급여인상 from emp;
select empno, ename, sal, decode(job,'SALESMAN',sal *1.1, 'CLERK', sal * 1.2, 'MANAGER' ,sal * 1.3) as 인상된급여 from emp;
select empno, ename,sal from emp where sal = (select max(sal) from emp where deptno in (10,20));
select empno, ename, sal from emp where sal = (select min(sal) from emp where deptno = 30);
select empno, ename, comm from emp where comm = (select max(comm) from emp);
select empno, ename, comm from emp where comm = (select min(comm) from emp);
select max(ename), min(ename) from emp;
select max(hiredate), min(hiredate) from emp;
select max(empno), min(empno) from emp;
select count(mgr) from emp;
select count(ename) from emp;
select count(*) from emp;
select count(*) from dept;
select count(comm) from emp;
select deptno, count(*) from emp group by deptno;
select mgr, count(*) from emp group by mgr;
select deptno, avg(sal) from emp group by deptno;
select deptno, stddev(sal) from emp group by deptno;
select deptno, job, sum(sal) from emp group by deptno, job;
select deptno, job, avg(sal) from emp group by deptno, job;
select deptno, job, max(hiredate), min(hiredate) from emp group by deptno, job;
select deptno, count(*) from emp group by deptno order by 2 desc;
select deptno, avg(sal) from emp group by deptno order by avg(sal);
select deptno,job,sum(sal) from emp group by deptno, job order by sum(sal) desc;
select deptno, job, avg(sal) from emp group by deptno, job order by avg(sal) desc;

select * from emp;
select * from deptno;
select * from salgrade;